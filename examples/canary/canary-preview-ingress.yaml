apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: canary-demo-preview
spec:
  ingressClassName: nginx
  rules:
  - host: test-rollout-preview.k8s.cagip.hackervaillant.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: canary-demo-preview
            port:
              number: 80
